<Window x:Class="PDFSplitterforCopilot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PDF Splitter For Copilot"
        Height="720"
        Width="1120"
        MinHeight="620"
        MinWidth="920"
        WindowState="Normal"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        AllowDrop="True"
        Drop="Window_Drop"
        DragOver="Window_DragOver"
        Background="{StaticResource FluentBackgroundBrush}"
        FontSize="14">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#E1DFDD"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#605E5C"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#0078D4"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#106EBE"/>
        <SolidColorBrush x:Key="AccentPressedBrush" Color="#005A9E"/>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="PanelCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="16"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentPressedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="{StaticResource FluentForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
        </Style>

        <Style x:Key="CompactInputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ToolbarButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="88"/>
            <Setter Property="Padding" Value="12,0"/>
        </Style>

        <Style x:Key="ToolbarPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Height" Value="34"/>
            <Setter Property="MinWidth" Value="112"/>
            <Setter Property="Padding" Value="14,0"/>
        </Style>

        <Style x:Key="ToolbarLabelStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="10,0,4,0"/>
        </Style>

        <Style x:Key="ToolbarComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="4,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ToolbarComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ToolbarHintStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Background="White" BorderBrush="#E1E1E1" BorderThickness="0,0,0,1">
            <MenuItem Header="File">
                <MenuItem Header="New" Click="MenuItem_New_Click"/>
                <MenuItem Header="Open..." Click="MenuItem_Open_Click"/>
                <MenuItem Header="Save" Click="MenuItem_Save_Click"/>
                <Separator/>
                <MenuItem Header="Exit" Click="MenuItem_Exit_Click"/>
            </MenuItem>
            <MenuItem Header="Edit">
                <MenuItem Header="Undo" Click="MenuItem_Undo_Click"/>
                <Separator/>
                <MenuItem Header="Cut" Click="MenuItem_Cut_Click"/>
                <MenuItem Header="Copy" Click="MenuItem_Copy_Click"/>
                <MenuItem Header="Paste" Click="MenuItem_Paste_Click"/>
            </MenuItem>
            <MenuItem Header="Options">
                <MenuItem Header="OpenAI API Settings..." Click="MenuItem_OpenAISettings_Click"/>
                <Separator/>
                <MenuItem Header="Font Size">
                    <MenuItem Header="Small (12px)" Click="MenuItem_FontSize_Click" Tag="12"/>
                    <MenuItem Header="Normal (14px)" Click="MenuItem_FontSize_Click" Tag="14" IsChecked="True"/>
                    <MenuItem Header="Large (16px)" Click="MenuItem_FontSize_Click" Tag="16"/>
                    <MenuItem Header="Extra Large (18px)" Click="MenuItem_FontSize_Click" Tag="18"/>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="Welcome" Click="MenuItem_Welcome_Click"/>
                <MenuItem Header="About" Click="MenuItem_About_Click"/>
            </MenuItem>
        </Menu>

        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#E1E1E1"
                BorderThickness="0,0,0,1"
                Padding="10,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            VerticalAlignment="Top"
                            Margin="0,0,16,0">
                    <StackPanel Orientation="Horizontal">
                        <Button Name="btnAddFiles"
                                Content="Add files"
                                Style="{StaticResource ToolbarButtonStyle}"
                                Margin="0,0,6,0"
                                Click="BtnAddFiles_Click"
                                ToolTip="Add PDF or Word files to the processing list."/>
                        <Button Name="btnRemoveSelected"
                                Content="Remove"
                                Style="{StaticResource ToolbarButtonStyle}"
                                Click="BtnRemoveSelected_Click"
                                ToolTip="Remove checked files from the list."/>
                    </StackPanel>
                    <CheckBox Name="cbGenerateRagJsonl"
                              Content="Advanced export: RAG JSONL"
                              IsChecked="False"
                              VerticalAlignment="Center"
                              Margin="0,6,0,0"
                              ToolTip="Also export RAG-ready JSONL chunks for downstream indexing."/>
                </StackPanel>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <WrapPanel Grid.Row="0" VerticalAlignment="Top">
                        <StackPanel Width="180" Margin="0,0,10,0">
                            <DockPanel LastChildFill="True">
                                <TextBlock Text="Task" Style="{StaticResource ToolbarLabelStyle}" Margin="0,0,4,0"/>
                                <ComboBox Name="cbOperation"
                                          Style="{StaticResource ToolbarComboBoxStyle}"
                                          ItemContainerStyle="{StaticResource ToolbarComboBoxItemStyle}"
                                          SelectedIndex="0"
                                          SelectionChanged="CbOperation_SelectionChanged"
                                          ToolTip="Choose the main task for the selected files.">
                                    <ComboBoxItem Content="Split PDF"/>
                                    <ComboBoxItem Content="Convert Word"/>
                                    <ComboBoxItem Content="Batch Convert Word"/>
                                </ComboBox>
                            </DockPanel>
                            <TextBlock Name="txtWorkflowHint"
                                       Style="{StaticResource ToolbarHintStyle}"
                                       Text="Split PDF by a fixed page count."
                                       Margin="0,4,0,0"/>
                        </StackPanel>

                        <StackPanel Width="170" Margin="0,0,10,0">
                            <DockPanel LastChildFill="True">
                                <TextBlock Text="Method" Style="{StaticResource ToolbarLabelStyle}" Margin="0,0,4,0"/>
                                <ComboBox Name="cbSplitMode"
                                          Style="{StaticResource ToolbarComboBoxStyle}"
                                          ItemContainerStyle="{StaticResource ToolbarComboBoxItemStyle}"
                                          SelectedIndex="0"
                                          SelectionChanged="CbSplitMode_SelectionChanged"
                                          ToolTip="Fixed pages splits by number. Context Split asks the LLM to propose page ranges.">
                                    <ComboBoxItem Content="Fixed pages"/>
                                    <ComboBoxItem Content="Context Split (LLM)"/>
                                </ComboBox>
                            </DockPanel>
                            <TextBlock Style="{StaticResource ToolbarHintStyle}"
                                       Text="Fixed or LLM-guided ranges."
                                       Margin="0,4,0,0"/>
                        </StackPanel>

                        <StackPanel Width="124" Margin="0,0,10,0">
                            <DockPanel LastChildFill="True">
                                <TextBlock Name="txtPagesLabel" Text="Pages" Style="{StaticResource ToolbarLabelStyle}" Margin="0,0,4,0"/>
                                <TextBox Name="numPageCount"
                                         Text="10"
                                         Style="{StaticResource CompactInputStyle}"
                                         PreviewTextInput="NumPageCount_PreviewTextInput"
                                         LostFocus="NumPageCount_LostFocus"
                                         ToolTip="Fixed pages: pages per output PDF. Context Split: target pages per suggested part."/>
                            </DockPanel>
                            <TextBlock Name="txtPageHint"
                                       Style="{StaticResource ToolbarHintStyle}"
                                       Text="Pages per output PDF."
                                       Margin="0,4,0,0"/>
                        </StackPanel>

                        <StackPanel Width="112" Margin="0,0,10,0">
                            <DockPanel LastChildFill="True">
                                <TextBlock Name="txtOverlapLabel" Text="Overlap" Style="{StaticResource ToolbarLabelStyle}" Margin="0,0,4,0"/>
                                <TextBox Name="numContextOverlap"
                                         Text="0"
                                         Style="{StaticResource CompactInputStyle}"
                                         PreviewTextInput="NumContextOverlap_PreviewTextInput"
                                         LostFocus="NumContextOverlap_LostFocus"
                                         ToolTip="Context Split only: include this many boundary pages on each side of a part."/>
                            </DockPanel>
                            <TextBlock Name="txtOverlapHint"
                                       Style="{StaticResource ToolbarHintStyle}"
                                       Text="Boundary pages."
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </WrapPanel>

                </Grid>

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            Margin="16,0,0,0"
                            VerticalAlignment="Top">
                    <Button Name="btnCancel"
                            Content="Cancel"
                            Style="{StaticResource ToolbarButtonStyle}"
                            Margin="0,0,6,0"
                            Visibility="Collapsed"
                            Click="BtnCancel_Click"
                            ToolTip="Cancel after the current file step finishes."/>
                    <Button Name="btnProcess"
                            Content="Run split"
                            Style="{StaticResource ToolbarPrimaryButtonStyle}"
                            Click="BtnProcess_Click"
                            ToolTip="Run the selected task for checked files."/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="10">
            <TextBlock Text="Drop PDF or Word files here"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="#A0A0A0"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Visibility="{Binding NoFiles, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <DataGrid Name="dgFiles"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      SelectionMode="Extended"
                      SelectionUnit="FullRow"
                      Background="White"
                      BorderBrush="{StaticResource PanelBorderBrush}"
                      BorderThickness="1"
                      RowBackground="White"
                      AlternatingRowBackground="#F9F9F9">
                                <DataGrid.ColumnHeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="Background" Value="#F3F2F1"/>
                                        <Setter Property="Foreground" Value="{StaticResource FluentForegroundBrush}"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Padding" Value="12,8"/>
                                        <Setter Property="BorderBrush" Value="#E1E1E1"/>
                                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                                    </Style>
                                </DataGrid.ColumnHeaderStyle>
                                <DataGrid.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Padding" Value="12,8"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGrid.CellStyle>
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="Use" Width="56">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                                          Tag="{Binding}"
                                                          HorizontalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Status" Width="64">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Ellipse Width="12" Height="12" Fill="{Binding StatusColor}"
                                                         HorizontalAlignment="Center"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="File" Width="3*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding FileName}"
                                                           TextTrimming="CharacterEllipsis"
                                                           ToolTip="{Binding FileName}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Path" Width="2*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding FilePath}"
                                                           TextDecorations="Underline"
                                                           Foreground="#0067B8"
                                                           Cursor="Hand"
                                                           MouseLeftButtonUp="FilePath_Click"
                                                           Tag="{Binding FilePath}"
                                                           TextTrimming="CharacterEllipsis">
                                                    <TextBlock.ToolTip>
                                                        <ToolTip>
                                                            <StackPanel>
                                                                <TextBlock Text="Open folder" FontWeight="Bold"/>
                                                                <TextBlock Text="{Binding FilePath}" TextWrapping="Wrap" MaxWidth="400"/>
                                                            </StackPanel>
                                                        </ToolTip>
                                                    </TextBlock.ToolTip>
                                                </TextBlock>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTextColumn Header="Pages" Binding="{Binding PageCount}" Width="80">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="TextAlignment" Value="Right"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <DataGridTemplateColumn Header="Progress" Width="1.6*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <ItemsControl Grid.Column="0" ItemsSource="{Binding Steps}"
                                                                  VerticalAlignment="Center" Margin="0,0,8,0">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <StackPanel Orientation="Horizontal"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Border BorderBrush="#D1D1D1" BorderThickness="1"
                                                                        Margin="1" CornerRadius="2">
                                                                    <Border.ToolTip>
                                                                        <ToolTip>
                                                                            <StackPanel>
                                                                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                                                                <TextBlock Text="{Binding Message}"/>
                                                                            </StackPanel>
                                                                        </ToolTip>
                                                                    </Border.ToolTip>
                                                                    <Rectangle Width="6" Height="12"
                                                                               Fill="{Binding StatusBrush}"
                                                                               RadiusX="1" RadiusY="1"
                                                                               Cursor="Hand"
                                                                               MouseLeftButtonUp="StatusBrush_Click"
                                                                               Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=DataGridRow}}"/>
                                                                </Border>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>

                                                    <TextBlock Grid.Column="1"
                                                               Text="{Binding StepMessage}"
                                                               VerticalAlignment="Center"
                                                               FontSize="12"
                                                               TextTrimming="CharacterEllipsis"/>
                                                </Grid>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTemplateColumn Header="Output" Width="150">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding OutputSummary}"
                                                           TextDecorations="Underline"
                                                           Foreground="#0067B8"
                                                           Cursor="Hand"
                                                           MouseLeftButtonUp="OutputSummary_Click"
                                                           Tag="{Binding}"
                                                           TextTrimming="CharacterEllipsis"
                                                           ToolTip="{Binding OutputTooltip}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Border Grid.Row="3"
                Background="White"
                Padding="10,8"
                BorderBrush="#E1E1E1"
                BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="260"/>
                </Grid.ColumnDefinitions>
                <TextBlock Name="txtStatus"
                           Text="Ready"
                           Foreground="{StaticResource FluentForegroundBrush}"
                           FontWeight="SemiBold"/>
                <Border Grid.Column="1"
                        Visibility="Collapsed"
                        Name="progressContainer"
                        Background="Transparent">
                    <ProgressBar Name="progressBar"
                                 Height="8"
                                 VerticalAlignment="Center"
                                 Background="#E1E1E1"
                                 Foreground="{StaticResource FluentAccentBrush}"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
